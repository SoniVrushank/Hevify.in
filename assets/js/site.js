document.querySelector(".nav-toggle")?.addEventListener("click",function(){const links=document.querySelector(".nav-links");const open=links.classList.toggle("open");this.setAttribute("aria-expanded",String(open));});const nav=document.querySelector(".site-nav");const onScroll=()=>nav?.classList.toggle("scrolled",window.scrollY>24);onScroll();addEventListener("scroll",onScroll,{passive:true});const io="IntersectionObserver"in window?new IntersectionObserver((entries)=>entries.forEach((entry)=>{if(entry.isIntersecting){entry.target.classList.add("in");io.unobserve(entry.target);}}),{threshold:.12}):null;document.querySelectorAll(".reveal").forEach((el)=>io?io.observe(el):el.classList.add("in"));document.addEventListener("click",(event)=>{const teamBtn=event.target.closest("[data-toggle-team]");if(teamBtn){const card=teamBtn.closest(".team-card");document.querySelectorAll(".team-card.open").forEach((item)=>{if(item!==card)item.classList.remove("open")});card.classList.toggle("open");}const detailBtn=event.target.closest("[data-open-detail]");if(detailBtn){const card=detailBtn.closest("[data-title]");const modal=document.getElementById("detail-modal");document.getElementById("detail-kicker").textContent=card.dataset.kicker||"Details";document.getElementById("detail-title").textContent=card.dataset.title||"Details";document.getElementById("detail-copy").textContent=card.dataset.copy||"";document.getElementById("detail-list").innerHTML=(card.dataset.list||"").split("|").filter(Boolean).map((x)=>"<li>"+x+"</li>").join("");document.getElementById("detail-mail").href=card.dataset.mail||"mailto:hevify.in@gmail.com";modal.classList.add("open");modal.setAttribute("aria-hidden","false");}if(event.target.matches("[data-close-modal]")||event.target.id==="detail-modal"){const modal=document.getElementById("detail-modal");modal.classList.remove("open");modal.setAttribute("aria-hidden","true");}});document.getElementById("mail-form")?.addEventListener("submit",(event)=>{event.preventDefault();const name=document.getElementById("mail-name").value.trim();const brand=document.getElementById("mail-brand").value.trim();const email=document.getElementById("mail-email").value.trim();const phone=document.getElementById("mail-phone").value.trim();const service=document.getElementById("mail-service").value;const message=document.getElementById("mail-message").value.trim();const body=["Hi Hevify Labs,","","I want to discuss my requirements.","","Name: "+name,"Brand: "+brand,"Email: "+email,"Phone: "+phone,"Service / Package: "+service,"Requirement: "+message,"","Please share the next steps.","","Thanks."].join("\n");location.href="mailto:hevify.in@gmail.com?subject="+encodeURIComponent("Requirement enquiry: "+service+" | Hevify Labs")+"&body="+encodeURIComponent(body);});function hydrateDetailFromCard(card){document.getElementById("detail-kicker").textContent=card.dataset.kicker||"Details";document.getElementById("detail-title").textContent=card.dataset.title||"Details";document.getElementById("detail-copy").textContent=card.dataset.copy||"";const source=card.dataset.packages||card.dataset.list||"";document.getElementById("detail-list").innerHTML=source.split(card.dataset.packages?"||":"|").filter(Boolean).map((x)=>{const parts=x.split("|");return parts.length>1?"<li><strong>"+parts[0]+"</strong><br><span>"+parts[1]+" plan</span><p>"+parts[2]+"</p><small>"+parts[3]+"</small></li>":"<li>"+x+"</li>";}).join("");document.getElementById("detail-mail").href=card.dataset.mail||"mailto:hevify.in@gmail.com";}document.addEventListener("click",(event)=>{const contactOpen=event.target.closest("[data-open-contact]");if(contactOpen){const modal=document.getElementById("contact-modal");modal.classList.add("open");modal.setAttribute("aria-hidden","false");}if(event.target.matches("[data-close-contact]")||event.target.id==="contact-modal"){const modal=document.getElementById("contact-modal");modal.classList.remove("open");modal.setAttribute("aria-hidden","true");}const btn=event.target.closest("[data-open-detail]");if(btn){const cats=document.getElementById("detail-cats");const current=btn.closest("[data-title]");const folderCards=[...document.querySelectorAll(".folder-card[data-title]")];cats.innerHTML=folderCards.length>1?folderCards.map((card,i)=>"<button type='button' data-folder-index='"+i+"' class='"+(card===current?"active":"")+"'>"+card.dataset.title+"</button>").join(""):"";hydrateDetailFromCard(current);}const cat=event.target.closest("[data-folder-index]");if(cat){const cards=[...document.querySelectorAll(".folder-card[data-title]")];const card=cards[Number(cat.dataset.folderIndex)];if(card){hydrateDetailFromCard(card);document.querySelectorAll(".modal-cats button").forEach((b)=>b.classList.remove("active"));cat.classList.add("active");}}});